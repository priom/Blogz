<%=	simple_form_for([@post, @post.comments.build]) do |f| %>
    <%=	f.input :name %>
    <%=	f.input :body, label: "Comment", :input_html => { 'rows' => 5}  %>

    <div class="col-md-4 col-md-offset-4">
        <%=	f.button :submit, class: "btn btn-default btn-block" %>
    </div>
<% end %>
